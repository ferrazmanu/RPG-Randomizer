import { Container } from "@/components/Elements/Container";
import * as S from "./styles";

export const Footer = () => {
  return (
    <Container>
      <S.Footer>
        Disclaimer: I don&apos;t own or created any of these lists, I only
        gathered the data and randomized it! The due credit of the data is on
        each page.
      </S.Footer>
    </Container>
  );
};
