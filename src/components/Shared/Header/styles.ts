import styled from "styled-components";

export const Header = styled.header`
  display: flex;
  gap: 16px;
  padding: 50px 0;

  ul {
    width: 100%;
    display: flex;
    gap: 16px;
    justify-content: space-around;
  }
`;
